1) مشروع Marketplace كامل موجّه للجالية السودانية

اسم المشروع: Sudanese Bazaar – متاجر السودان
الوصف:
منصة متكاملة لبيع المنتجات السودانية، التوابل، الملابس، الإكسسوارات، الأطعمة، المنتجات المنزلية، مع نظام شحن وتوصيل داخل الكويت والخليج.
هدف التطبيق: ربط السودانيين بالتجار السودانيين، ودعم المتاجر السودانية إلكترونياً.

📱 2) Customer Mobile App (React Native + TypeScript)

قم ببناء تطبيق عميل كامل يحتوي على:

✔ المزايا الأساسية

تسجيل الدخول / التسجيل (Email – Phone – OTP).

الصفحة الرئيسية (بانرات – عروض – فئات – منتجات مشهورة).

بحث متقدم + فلاتر (النوع – السعر – التقييم).

صفحة منتج احترافية:

صور متعددة

خيارات الحجم واللون

وصف

تقييمات Reviews

وظيفة Wishlist.

Cart + Checkout كامل.

إضافة العنوان (الكويت + الخليج).

طرق الدفع:

Cash

Online payment placeholder (KNET/Stripe Ready).

تتبع الطلب Order Tracking.

صفحة الطلبات السابقة.

إشعارات FCM.

دعم اللغة العربية + الإنجليزية.

Dark / Light mode.

تصميم حديث مناسب للجالية السودانية (ألوان: Gold + Green + White).

🛒 3) Seller Panel (Web or Mobile) — للتجار السودانيين

قم ببناء لوحة تحكم للبائعين:

تسجيل الدخول كبائع.

إنشاء وتعديل المنتجات.

رفع صور المنتجات.

إدارة المخزون.

إدارة الطلبات.

الرد على استفسارات العملاء.

تقارير المبيعات.

إدارة حساب البائع.

تقنيات الواجهة:
React.js + TailwindCSS + TypeScript

🚚 4) Driver App (اختياري ولكن مفضل جدًا)

تطبيق بسيط للسائقين السودانيين:

تسجيل الدخول.

عرض الطلبات المخصّصة للسائق.

تحديث حالة الطلب:

تم الاستلام

في الطريق

تم التوصيل

خريطة التوصيل (Mapbox أو Google Maps Ready).

إشعارات فورية.

React Native TypeScript.

🛠 5) Admin Dashboard (Web)

لوحة تحكم لإدارة النظام بالكامل:

إدارة العملاء.

إدارة البائعين.

إدارة المنتجات.

إدارة الطلبات.

إدارة الفئات Categories.

إدارة الإعلانات والبانرات.

إدارة العروض والخصومات.

تقارير عن البيع – الأرباح – النشاط.

صلاحيات متعددة (super admin – support – finance).

React + Tailwind + TypeScript + Vite

🔧 6) Backend (Node.js + TypeScript — Prefer NestJS)

قم ببناء Backend كامل يحتوي على:

Modules

Users

Sellers

Drivers

Products

Categories

Variants

Cart

Orders

Payments

Notifications

Reviews

Shipping zones

Address book

التقنيات

JWT Authentication

Prisma ORM

PostgreSQL أو MySQL

File Upload (Cloudinary أو Local Storage)

Socket.io للتحديث الفوري

Swagger API documentation

Webhooks جاهزة للربط مع الدفع

🧱 7) Database Requirements

قم بإنشاء:

ERD كامل

جداول عملاء – بائعين – سائقين – منتجات – طلبات – عربة – تقييمات

العلاقات Many-to-Many للبائعين والمنتجات

Migration scripts

🎨 8) UI/UX Requirements

تصميم يشبه Amazon + Noon

لكن بلمسة سودانية (ألوان علم السودان + صور منتجات سودانية)

تجربة استخدام سهلة جداً

Animations خفيفة

Skeleton loaders

📦 9) Required Deliverables

يجب أن تسلمني:

الكود النهائي الكامل (React Native TS + Backend + Seller + Admin + Driver).

ملفات المشروع في مجلد ZIP.

قاعدة بيانات SQL جاهزة للاستيراد.

ملفات .env template.

دليل تشغيل كامل:

تشغيل التطبيق

تشغيل الـ Backend

تشغيل لوحة التحكم

تشغيل Seller Panel

Screenshots + Video Demo (إن أمكن).

Theme + Colors + Fonts.

API Documentation باستخدام Swagger.

🧠 10) Architecture Requirements

Clean Architecture

SOLID

Reusable components

Redux Toolkit + RTK Query

Error handling

Validation strict

Type-safe APIs

Secure authentication

✔ Key Improvements

دمج 4 تطبيقات + Backend في برومبت واحد.

توضيح أن النظام موجّه للجالية السودانية.

إضافة Driver App + Seller App + Admin Dashboard.

تحديد لغات البرمجة المطلوبة بدقة.

تحديد مخرجات واضحة للـ Agent.

هيكلة كاملة شبيهة بأمازون.